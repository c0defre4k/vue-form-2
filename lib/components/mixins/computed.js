import fieldClasses from '../computed/field-classes'
import hasFeedback from '../computed/has-feedback'
import feedbackIcon from '../computed/feedback-icon'
import validationError from '../computed/validation-error'
import success from '../computed/success'
import hasLabel from '../computed/has-label'
import trigger from '../computed/trigger'

export default {
  computed: {
    fieldClasses,
    hasFeedback,
    feedbackIcon,
    validationError,
    success,
    hasLabel,
    trigger
  }
}
